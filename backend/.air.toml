root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main.exe ."
  entrypoint = "./tmp/main.exe"
  include_ext = ["go"]
  exclude_dir = ["tmp", "migrations"]
  exclude_regex = ["_test.go"]
  delay = 300

[log]
  time = true
